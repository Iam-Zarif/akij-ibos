export type RecruiterOnlineTest = {
  id: string;
  title: string;
  candidates: string;
  questionSet: string;
  examSlots: string;
};

export type DeveloperOnlineTest = {
  id: string;
  title: string;
  duration: string;
  questions: string;
  negativeMarking: string;
};
